export enum GridFilterType {
  Range = 1,
  Match = 2,
  Contains = 3,
}

export enum RoleTypes {
  Customer = 0,
  Admin = 1,
}

export enum ImageType {
  Generic = 0,
  Product = 1,
}
